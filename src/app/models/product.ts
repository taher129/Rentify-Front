import {Category} from "./category";

export class Product {
  id?: number;
  name?: string;
  price?: number;
  description?: string;
  userId?: number;
  categoryId?: number;
  productImage?: string;
}
